---
id: format-prd-markdown
type: format
how: code
when: after the model returns
---

Turn the JSON fields into a fixed Markdown document in `agent.py`.
The model must not emit the Markdown. Heading structure stays stable
so spec and reviewers can quote the same document.
