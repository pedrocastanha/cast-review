---
id: format-markdown
type: format
how: code
when: always
---

Build Portuguese Markdown with fixed headings: PRD, Implementation Spec,
then each reviewer score and finding. Empty lists render as nenhum/nenhuma.
This skill is the body of `agent.py`.
